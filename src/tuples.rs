fn main() {
    let tup = (500, 6.4, 1);

    let (x, y, z) = tup;

    println!("The value of y is {}", y);
}

// fn poop() {
//     let tup: (i32, f64, u8) = (500, 6.4, 1);
// }

// This is a tuple. Itis a comma-separate list of values inside parentheses


// To get individual values out of a tuple, use patern matching:
